export const TEST_FONT = 'SnapshotTest';

export function testFont(size: number): string {
  return `${TEST_FONT} ${size}`;
}

export function testBoldFont(size: number): string {
  return `${TEST_FONT} ${size}`;
}
