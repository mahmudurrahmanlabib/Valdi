//
// Greeting.swift
//

/**
* Generated from type 'Greeting' at 'binding_lang_both/src/NativeApi.d.ts' by the Valdi compiler.
* Please do not edit this file directly.
*/
import ValdiCoreSwift
public enum Greeting: Int32, ValdiMarshallableIntEnum {
    case casual = 1
    case formal = 2

    public init(from marshaller: ValdiMarshaller, at objectIndex: Int) throws {
        self = try createIntEnum(from:marshaller, at:objectIndex)
    }
}
public func register_Greeting() {
    let enumCases: [Int32] = allEnumCases(of: Greeting.self)
    ValdiMarshallableObjectRegistry.shared.registerIntEnum(enumName: "Greeting", enumCases: enumCases)
}


//
// IGreeter.swift
//

import ValdiCoreSwift

/**
* Generated from type 'IGreeter' at 'binding_lang_both/src/NativeApi.d.ts' by the Valdi compiler.
* Please do not edit this file directly.
*/
public protocol IGreeter : ValdiMarshallableInterface {
    func greet(name: String, style: Greeting) throws -> String
        func count() throws -> Double
}


extension IGreeter {
    public func push(to marshaller: ValdiMarshaller) throws -> Int {
        let objectIndex = try marshaller.pushProxy(object: self) {
        register_IGreeter()
        let objectIndex = try marshaller.pushObject(className: "IGreeter")
        try marshaller.setTypedObjectPropertyFunction(objectIndex, 0, Self.create_greet_bridgeFunction(using: self.greet))
        try marshaller.setTypedObjectPropertyFunction(objectIndex, 1, Self.create_count_bridgeFunction(using: self.count))
        }
        return objectIndex
    }

    static func create_greet_bridgeFunction(using functionImpl: @escaping ((String, Greeting) throws -> String)) -> ((ValdiMarshaller) -> Bool) {
        
        return createBridgeFunctionWrapper({ marshaller in
            let name : String = try marshaller.getString(0)
                let style : Greeting = try marshaller.getEnum(1)
            let result = try functionImpl(name, style)
            _ = marshaller.pushString(result)
            })
    }
    static func create_greet_from_bridgeFunction(using functionImpl: ValdiFunction) -> ((String, Greeting) throws -> String) {
        
        return { (name: String, style: Greeting) throws -> String in
            let marshaller = ValdiMarshaller()
            _ = marshaller.pushString(name)
            _ = marshaller.pushEnum(style)
            if !functionImpl.perform(with: marshaller, sync: true) {
                try marshaller.checkError()
                throw ValdiError.functionCallFailed("greet")
            }
            let __result: String = try marshaller.getString(-1)
            try marshaller.checkError()
            return __result
        }
    }
    static func create_count_bridgeFunction(using functionImpl: @escaping (() throws -> Double)) -> ((ValdiMarshaller) -> Bool) {
        
        return createBridgeFunctionWrapper({ marshaller in
            
            let result = try functionImpl()
            _ = marshaller.pushDouble(result)
            })
    }
    static func create_count_from_bridgeFunction(using functionImpl: ValdiFunction) -> (() throws -> Double) {
        
        return { () throws -> Double in
            let marshaller = ValdiMarshaller()
            
            if !functionImpl.perform(with: marshaller, sync: true) {
                try marshaller.checkError()
                throw ValdiError.functionCallFailed("count")
            }
            let __result: Double = try marshaller.getDouble(-1)
            try marshaller.checkError()
            return __result
        }
    }

}
public class IGreeter_Proxy : IGreeter, ValdiMarshallableObject {
    public func greet (name: String, style: Greeting) throws -> String {
        
        return try self.greet_implementation(name, style)
    }
    private var greet_implementation: ((String, Greeting) throws -> String)
    public func count () throws -> Double {
        
        return try self.count_implementation()
    }
    private var count_implementation: (() throws -> Double)

    public required init(from marshaller: ValdiMarshaller, at objectIndex: Int) throws {
        let objectIndex = try marshaller.unwrapProxy(objectIndex)
        self.greet_implementation = Self.create_greet_from_bridgeFunction(using: try marshaller.getTypedObjectPropertyFunction(objectIndex, 0))
        self.count_implementation = Self.create_count_from_bridgeFunction(using: try marshaller.getTypedObjectPropertyFunction(objectIndex, 1))
        marshaller.pop()
    }

    static func getDescriptor() -> ValdiMarshallableObjectDescriptor {
        let fields: [ValdiMarshallableObjectFieldDescriptor] =
        [
            ValdiMarshallableObjectFieldDescriptor(name: "greet", type: "f(s, r<e>:'[0]'): s"),
            ValdiMarshallableObjectFieldDescriptor(name: "count", type: "f(): d"),
        ]
        let identifiers: [ValdiMarshallableIdentifier] =
        [
            ValdiMarshallableIdentifier(name:"Greeting", registerFunc: register_Greeting),
        ]

        return ValdiMarshallableObjectDescriptor(fields, identifiers: identifiers, type: ValdiMarshallableObjectType.Interface)
    }

}
public func register_IGreeter() {
    ValdiMarshallableObjectRegistry.shared.register(className: "IGreeter", objectDescriptor: IGreeter_Proxy.getDescriptor())
}

